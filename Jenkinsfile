pipeline {
 agent any
 stages {
    stage("build") {
         steps {
             bat 'echo build -----'
         }
    }
     stage("test") {
         steps {
             bat 'echo test ------'
         }
     }
 }
}
